@model WEB_MEDICAL.ViewModels.PatientView

@{
    ViewData["Title"] = "ShowPatient";
}

<h1>ShowPatient</h1>

<h4>DepartmentView</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="PatientResult">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="DepartmentId" class="control-label"></label>
                <select asp-for="DepartmentId" class="form-control" asp-items="Model.DepartmentsList">
                    <option value="">select department</option>
                </select>
                <span asp-validation-for="AdmitDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Show Patient" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
